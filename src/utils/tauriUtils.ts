

/**
 * tauri 下载文件
 * @param name
 * @param url
 * @param data
 */
export const tauriDownFile = async (name: string, url: string, data: object = {}) => {
	console.log(name)
	console.log(url)
	console.log(data)
}